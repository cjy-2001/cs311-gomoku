def minimax(self, maximizing: bool):
    #base case
    if (board is at terminal state or current board reaches the depth limit):
        count black's and white's shapes,
        which is number of open or half fours, threes, and twos
        apply evaluation function on the shape difference to calculate score
        return score

    #recursion
    scores = []
    for board in board.get_possible_moves():
        scores.append(board.minimax(!maximizing))

    if maximizing:
        return max(scores)
    else:
        return min(scores)